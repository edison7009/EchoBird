{
  "name": "codex-provider-sync-vendored",
  "version": "0.2.5",
  "description": "Vendored copy of Dailin521/codex-provider-sync — keeps Codex's per-session metadata aligned with the active provider so historical chats stay visible after switching models. Invoked silently from apply_codex (Rust). DO NOT edit src/ — re-vendor from upstream instead; see UPSTREAM.txt.",
  "type": "module",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=24.0.0"
  }
}
